---
kind: plan
status: active
phase: v0.14
source: SPEC.md
priority: high
---

# v0.14：沙箱 + 信任基础（工作空间治理落地）

## 目标

落实"工作空间与沙箱（治理）"与"信任边界"定案（SPEC.md / AUTHORITY.md）：
进程私密性的机械化 + 信任基础落地。

## 环境前置（已实测，不单开卡）

`bwrap --ro-bind / / --unshare-all true` 本机通过（bwrap + user/pid
namespace 可用，Ubuntu 24.04）。

## 范围（TODO 卡，现 5 张 + DONE 1 张）

| 卡 | 复杂度 | 内容 |
|---|---|---|
| data-dir-convention | S | ✅ DONE：数据区 `data/<identity>` 约定落地（agent workdir / 设备） |
| sandbox-wrapper | L | bwrap 重执行 + userns/pidns/netns/ipcns + setrlimit + 系统只读 + 数据区写根 + 默认禁网（**固定矩阵，不承载权限**）；**传输层重写为 fd 继承 Connection（沙箱内零共享 IPC）+ 宿主读侧盖章（原 outbound-channel 内容并入）** |
| network-declaration | M | needs_network 显式声明机制（默认禁网） |
| bash-sandbox-adapt | M | bash 设备沙箱语义 + PROTOCOL 更新（cwd 落数据区；data_dir 经内核环境变量注入设备） |
| sandbox-verification | M | 沙箱+出站通道故事测试 + 全量回归 + 文档一致性收口 |

## 依赖

data-dir-convention → sandbox-wrapper → bash-sandbox-adapt → verification；
network-declaration 在 sandbox-wrapper 后（编辑边界实际重叠：_bwrap_args 与
构造参数链同文件，**串行**）；outbound-channel 内容已并入 sandbox-wrapper
（读侧盖章随传输层重写落地，不再单列）；全卡 → verification 收口。落文档
任务不入卡（随提交直接对齐，提交前核对一致性）。

## 验收

- 所有设备进程默认进沙箱：系统路径只读、数据区唯一写根、默认禁网、
  setrlimit 受限、userns/pidns/netns/ipcns 阻断对兄弟/内核进程的信号、
  内存、网络与 System V IPC 攻击（Yama 兜底 ptrace）；沙箱内除继承 fd
  外无跨进程通道；
- 沙箱为固定矩阵（家 + 只读系统），无按 position 的挂载物化；
- 设备持久化数据互不干扰（FS 级强制）；
- bash 命令沙箱内执行且语义正确（cwd/输出落数据区；setsid 逃逸被
  pid namespace + `--die-with-parent` 解决）；
- 出站通道：子进程内不存在可改写的身份字段；伪造 source 尝试被内核
  读侧盖章覆盖；宿主直投路径保留；
- 文档（SPEC/AUTHORITY.md）与代码进度一致；
- 全量回归（tmp/check_*.py + demo）PASS。

## 范围外（明确不做）

- 设备间调用与 origin（AUTHORITY.md 开放问题 11）；
- 运行期状态持久化（开放问题 7）；
- 服务账户 Agent、凭证管理（信任触发条件驱动的未来项）。
