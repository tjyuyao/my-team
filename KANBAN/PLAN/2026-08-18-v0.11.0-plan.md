---
kind: plan
status: active
phase: v0.11 场景
source: 拆分自 v0.9.0-plan（2026-08-17 原版，拆分 commit 90cd5a8）+ 扩展表面实现计划（并入）
---

# v0.11.0 计划 — 扩展表面语义闭合 + 场景包

**Date:** 2026-08-18
**Status:** 计划中（v0.10 门通过后启动）

## 版本定位

v0.11 的核心交付是**扩展表面 P0 语义闭合**（ProcessDef/Authority/
Recovery/ExecutionProfile/Trust Boundary/Predicate/Validator，设计规范
见 `OPEN_ISSUE/2026-08-18-extension-surface-spec.md`）；五场景包、Skill
等场景资产在语义闭合后交付，且受测试向量纪律约束。

```text
v0.11 — 扩展表面 P0 语义闭合（核心，7 个 TODO）
        + 场景包/Skill（依赖语义闭合，延后）
        + v0.8 遗留收尾 / MCP 接入（可并行）
```

扩展表面是跨版本方向：P0 = 本版本核心交付；P1/P2（自进化风险分级、
多 Deployment、embedding 等）属 v1.0+，列于下文 backlog，待 v1.0 计划
出现时落位。

## 最小测试向量（贯穿所有任务的唯一判据）

```text
IngressEvent → ProcessInstance → assignment → human approval
→ external irreversible operation → crash recovery
→ compensation / reconciliation
```

这条链路在语义上闭合之前，不实现任何场景资产（软件公司/小说工作室/电商/
自媒体/知识星球）。

## TODO 清单（详见 KANBAN/TODO/）

### 扩展表面 P0（核心，阻塞层）

| # | 文件 | 内容 | 状态 |
|---|---|---|---|
| E1 | `KANBAN/TODO/process-model` | ProcessDef/ProcessInstance/Gate schema + 状态机 | 待实现 |
| E2 | `KANBAN/TODO/authority-evaluation` | DecisionClaim + 裁决算法（核心已实现，见注） | 接点待集成 |
| E3 | `KANBAN/TODO/pending-outbox-recovery` | pending op 生命周期 + outbox 恢复 + unknown/对账 | 待实现 |
| E4 | `KANBAN/TODO/execution-profile` | ExecutionProfile + 版本绑定 | 待实现 |
| E5 | `KANBAN/TODO/package-trust-boundary` | 三类资产边界 + 签名/命名空间 | 待实现 |
| E6 | `KANBAN/TODO/predicate-dsl` | 谓词 L0/L1/L2 边界 | 待实现 |
| E7 | `KANBAN/TODO/static-validator` | 五类 + 可达性/终局/单调/数据流/资源 | 待实现 |

> E2 注：`authority.py` 核心（8 域/7 元组/context 匹配/composition/
> escalation/委派单调静态检查）已独立实现并有 32 测试；剩余接点
> （与 simulation.py Validate/Commit 集成）依赖 E1 落地。

### 版本交付（依赖语义闭合）

| # | 文件 | 内容 | 状态 |
|---|---|---|---|
| 13 | `KANBAN/TODO/scenario-packages` | 场景包系统与 demo | **待重划** |
| 17 | `KANBAN/TODO/mcp-provider-adapter` | MCP server → ToolManifest 接入 | 小改 |
| 18 | `KANBAN/TODO/skill-package-system` | Skill Package 系统（面向非专业用户） | **待重划** |

## 待重划项（与扩展表面方向冲突，须先改 TODO 再实现）

- **T13 scenario-packages**：① `tools/` = manifest + handler/executor
  引用违反 P0-6（配置包不得携带可执行能力）；② `approval_policies.json`
  /`scenario.json` 旧结构被 Authority+gate+OperationPolicy 取代；
  ③ "首个 demo 做软件公司场景包"违反测试向量纪律（场景资产延后）。
  须改为：场景包=配置包 + 引用能力包，INSTALL_PACKAGE 事务安装 +
  静态校验器（五类+可达性/数据流），demo 待测试向量闭合。
- **T18 skill-package-system**：① 按关键词把 SOP/模板/知识注入 briefing
  是提示词注入与错误激活风险（审阅 §七），须改结构化来源段
  `[UNTRUSTED_CUSTOMER_CONTENT]/[SKILL_INSTRUCTION]/[POLICY]` +
  优先级；② `scripts/` 独立为能力包，Skill 只引用已有 Tool；
  ③ Skill 示例属场景资产，待测试向量闭合。
- **T17 mcp**（小改）：补"adapter = 能力包（Provider 安装+签名）"信任
  框架表述；其余（deny-by-default、内核注入 agent_id）方向正确。
- **SPEC 对齐**：§11 场景包结构、§11.4 Skill 先行对齐，再改 TODO。

## 依赖关系

```text
v0.10 边界能力（T7–T12）
    ↓
扩展表面 P0（E1–E7）：
  E1 process-model ────────────┐
  E2 authority-evaluation ─────┼──→ E7 static-validator
  E3 pending-outbox-recovery ──┤         ↑
  E4 execution-profile ────────┤         │
  E5 package-trust-boundary ───┤         │
  E6 predicate-dsl ────────────┘         │
                                         │
  E4 / E5 ───────────────────────────────┘
    ↓
T13 场景包（重划后）/ T18 Skill（重划后）← 依赖扩展表面语义闭合
T16 遗留 / T17 MCP（可并行，不受扩展表面阻塞）
```

- E1 process-model 是其余任务的对象载体（ProcessInstance 绑定 profile、
  authority、pending op），先行。
- E2 与 E3 可并行（前者管裁决，后者管可靠性）。
- E4 依赖 E1（profile 被 ProcessInstance 引用）。
- E5 与 E6 独立，可并行。
- E7 static-validator 收尾：依赖 E1–E6 的 schema 定型（校验的是它们的
  良构性）。

## P1 backlog（进入 v1.0 前，未拆 TODO）

1. 包签名、依赖、命名空间、api_compatibility 完整化（E5 已含骨架）。
2. 并行分支 / join / 重试 / 超时 / 取消 / 补偿的运行时语义（E1 已含状态机，
   此为实现）。
3. root 自进化的风险分级（低/中/高：谁可自动灰度、谁须 Owner 审批）；
   **authorization 永不扩大、闭包永不可改、裁决链不可绕过 Owner**。
4. Skill 的 prompt 注入隔离落到 ContextCompiler（结构化来源段）。
5. `effective_tick` 发布语义落到 runtime（E4 已含 schema）。
6. 静态校验器接入 INSTALL_PACKAGE 加载路径 + 生成时（root 产物）路径。
7. Identity 闭包落地（from/to 身份字段内核注入、不可伪造，SPEC §12.1；
   v0.9 已删 identity.py，当前无独立模块）——支撑 Human Worker（v0.10
   T12a）与审批/三查（E1/E2）的身份认证。

## P2 backlog（后续增强，v1.0+，待 v1.0 计划落位）

子流程/subgraph 嵌套 · route 灰度与 A/B · ProcessInstance 显式迁移 ·
多 Deployment 扩展 · embedding 型 KB 检索 · Authority 动态条件与 threshold。

## 验收门

- v0.11 门：至少一个场景包端到端 demo 运行，内核代码零改动
  （受测试向量纪律约束：语义未闭合前不启动场景资产）。
- 每个 TODO 独立可验收；`uv run pytest -q` 全绿 + ruff/mypy。
- 重划项在实现前完成 TODO 重写；不按旧设计开工。
- 验收不变量（每项对应审阅"验收不变量"）：
  - [ ] 任一 ProcessInstance 的全部运行时语义绑定到一个不可变 ExecutionProfile
  - [ ] 新 PackageVersion 发布不改变既有 ProcessInstance 结构
  - [ ] Commit 成功但 Publish 未完成时，重启后可恢复 outbox（稳定幂等键）
  - [ ] 外部调用结果为 `unknown` 时，未经确认不重复执行不可逆操作
  - [ ] 任意 gate 的拒绝/超时/未决都有终止路径或显式 escalation
  - [ ] 任意 predicate 纯、有限、可审计、可重放
  - [ ] root 不能扩大自身 authorization，不能绕过 Owner escalation
  - [ ] 配置包不能携带未声明/未签名/未授权的可执行能力
  - [ ] 同 Deployment 内私有记忆/凭证/资产引用不交叉泄漏
  - [ ] 角色变化不伪造/覆盖 Principal Identity
  - [ ] 外部事件重复投递不创建重复 ProcessInstance（除非流程显式声明允许）
  - [ ] 任一 effect 可追溯到 Intent、ProcessInstance、PackageVersion、Principal
  - [ ] 最小测试向量端到端闭合，内核代码零改动

## 明确不做（本版本）

- 五个场景包的资产实现（测试向量闭合前）
- embedding 型 KB 检索（P2）
- 多 Deployment 运行时共享（配置经验可复用，运行时隔离）
