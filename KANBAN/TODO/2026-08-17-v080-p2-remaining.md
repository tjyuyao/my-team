# v0.8 遗留 P2：run_tests 真隔离、Snapshot 矩阵、token/cost 预算

**Phase:** v0.10（v0.8 P2 遗留收尾；不依赖扩展表面，可并行）
**Source:** SPEC §6.3、§14；KANBAN/DONE/2026-08-18-v0.8.0-implementation-plan.md（P2-7/8/11）
**Priority:** medium

## 目标
收掉 v0.8.0 计划中未完成的 P2-7/8/11 三项，作为 v0.9/v0.10 的
前置或并行任务。

## 要求 / 规则
- P2-7 run_tests 真实隔离：只读挂载（临时工作区副本）、网络
  deny-by-default、资源限制（CPU/内存/进程数/文件大小）、环境净化
  （sitecustomize/PYTHONPATH/PATH/secret 剥离）、GIT_* 固定；
  达成后 run_tests 执行类升级为 SANDBOXED_PROCESS。
- P2-8 Snapshot 覆盖矩阵：TaskTree / Scheduler claims / Pending ops /
  Private files 版本视图 / Shared KB / 外部进程 / LLM 请求 / ID 分配 /
  state_epoch 逐行验证 Freeze 可见性 / Commit 可回滚性 / 持久化。
- P2-11 token/cost 预算：定价表 + 每 agent/task/simulation 上限，
  PreValidate 拒绝；concurrency / request_count / token / cost /
  wall_time 分列。

## 产出
- SANDBOXED_PROCESS 执行等级与 run_tests 升级。
- Snapshot 矩阵测试文件。
- 预算模型与 PreValidate 集成。

## 验收标准
- [ ] run_tests 在只读挂载 + 网络拒绝 + 资源限制下执行
- [ ] Snapshot 矩阵全部行通过（测试可见）
- [ ] 超过 token/cost 预算的 LLM 请求在 PreValidate 被拒绝
- [ ] `uv run pytest -q` 全绿；`ruff`/`mypy` 通过
