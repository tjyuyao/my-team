# My-Team 项目开发约定

- 风格、品味与纪律：
    - 人类开发者：他很讨厌你以下位者的奉承立场一起做事，所以请保持平等、客观、批判性的立场。
    - 代码品味：极简、改造、合并而非增加结构，代码结构最小化，追求高内聚、低耦合原则，正确性优先，历史决策如果应该被推翻，则需要向正确性让步，不计代价，因为如果不处理代价会越来越高，不向后兼容，破坏性更新，所有的事情只提供唯一一种做法，代码洁癖，语义用词洁癖，强迫症，禁止静默捕获异常，能不提供默认值的情况下就不要提供默认值
    - 进攻型编程：崩溃是免费的缺陷报告——问题被暴露出来，迟早会被修掉（无需额外入文档）；真正的敌人是隐形失败：阻塞、静默挂掉、吞异常、空心断言。宁可大声失败让问题可见，也不要"优雅"地静默退化
    - 使用 uv 处理虚拟环境
    - git add 要显式指定，不允许使用 -A 选项，项目具有两层 git （phynovel 为子模块）。

- 核心文档集:
    - `SPEC.md` 最新版总体设计文档
    - `KANBAN/README.md` 进度管理看板文档

- 多 agent 协作：
    - 项目开发应该积极采用 subagent，目的不是增加并行性，而是保持主 agent 上下文干净，使 agent 可以专注于负责宏观目标把控、质量把控和任务编排。
    - 主 agent 在布置任务时，应该明确告知 subagent 其身份和职责。
    - 任何类型的任务，主 agent 只关心结果，不关心过程的，都应该交给 子 agent 去实施。
    - 如果你是子 agent，多 agents 同时处理本项目开发中的不同任务/不同文件是常遇到的情况，通常可以靠任务隔离保证，但需要格外注意工作边界。