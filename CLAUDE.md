# My-Team 项目开发约定

- 代码品味：极简、改造、合并而非增加结构，代码结构最小化，正确性优先，不向后兼容，破坏性更新，所有的事情只提供唯一一种做法，代码洁癖，语义用词洁癖，强迫症，禁止静默捕获异常，能不提供默认值的情况下就不要提供默认值
- 此文件只能由人类程序员修改
- 使用 uv 处理虚拟环境
- git add 要显式指定，不允许使用 -A 选项
- 必读 `SPEC.md` 和 `KANBAN/README.md`
- 项目开发应该积极采用 subagent 来保持上下文干净。主 agent 负责宏观目标把控和任务编排。主 agent 在布置任务时，应该明确告知 subagent 其身份和职责：如代码实现、代码质量审查、回归测试质量审查。
- 多 Agents 同时处理本项目开发中的不同任务/不同文件是常遇到的情况，通常可以靠任务隔离保证，但需要格外注意工作边界